# sn-example-terraform

You shouldn't create a new repository using this template. Instead, you can copy the `terraform` folder into your
repository and then just copy the CircleCI config into your config.
